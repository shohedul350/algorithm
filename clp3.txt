CREATE TABLE student (
    ID int NOT NULL PRIMARY KEY AUTO_INCREMENT,
    name varchar(255),
    mark int,
    roll int,
    city varchar(255)
);


1. SELECT name, mark FROM student WHERE mark = (SELECT MAX(mark) FROM student);
